<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pontes_stefane_esig.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.CardFormActivity"
            android:screenOrientation="sensor" />
        <activity
            android:name=".activities.ListtFormActivity"
            android:screenOrientation="sensor" />
        <activity
            android:name=".activities.HomeActivity"
            android:screenOrientation="sensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.ProjectFormActivity"
            android:screenOrientation="sensor" />
        <activity
            android:name=".activities.ProjectActivity"
            android:screenOrientation="sensor" />
    </application>

</manifest>